<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lj.iot.biz.db.smart.mapper.ParameterConfigMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lj.iot.biz.db.smart.entity.ParameterConfig">
        <id column="id" property="id" />
        <result column="dictionary_key" property="dictionaryKey" />
        <result column="dictionary_value" property="dictionaryValue" />
        <result column="dictionary_remark" property="dictionaryRemark" />
        <result column="creater" property="creater" />
        <result column="create_time" property="createTime" />
        <result column="updater" property="updater" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, dictionary_key, dictionary_value, dictionary_remark, creater, create_time, updater, update_time
    </sql>

    <select id="customPage" resultMap="BaseResultMap">
        select * from parameter_config
        <where>
            <if test="params.search!=null and params.search!=''">
                AND (
                dictionary_key like concat(#{params.search},'%')
                or dictionary_value like concat(#{params.search},'%')
                or dictionary_remark like concat(#{params.search},'%')
                )
            </if>
        </where>
        order by id desc
    </select>

</mapper>
