<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lj.iot.biz.db.smart.mapper.HotelUserRoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lj.iot.biz.db.smart.entity.HotelUserRole">
        <id column="id" property="id"/>
        <result column="hotel_id" property="hotelId"/>
        <result column="hotel_user_id" property="hotelUserId"/>
        <result column="role_id" property="roleId"/>
    </resultMap>

    <select id="queryRoleIdList" resultType="Long">
        select role_id
        from hotel_user_role
        where hotel_user_id = #{hotelUserId}
          and hotel_id = #{hotelId}
    </select>

</mapper>
