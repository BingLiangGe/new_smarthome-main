<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lj.iot.biz.db.smart.mapper.RfModelMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lj.iot.biz.db.smart.entity.RfModel">
        <id column="id" property="id"/>
        <result column="device_type_id" property="deviceTypeId"/>
        <result column="brand_id" property="brandId"/>
        <result column="brand_name" property="brandName"/>
        <result column="model_name" property="modelName"/>
        <result column="head_data" property="headData"/>
        <result column="code_type" property="codeType"/>
        <result column="start_zero_time" property="startZeroTime"/>
        <result column="unit_time" property="unitTime"/>
        <result column="sent_count" property="sentCount"/>
    </resultMap>

    <select id="customPage" resultMap="BaseResultMap">
        select * from rf_model
        <where>
            <if test="params.search!=null and params.search!=''">
                AND (
                brand_name LIKE CONCAT(#{params.search},"%")
                OR model_name LIKE CONCAT(#{params.search},"%")
                )
            </if>
        </where>
        ORDER BY id DESC
    </select>
</mapper>
